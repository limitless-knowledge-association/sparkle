#!/usr/bin/env node

/**
 * Bake the version into src/version.js
 * This runs during "npm pack" via the prepack script
 */

import { readFile, writeFile } from 'fs/promises';
import { join, dirname } from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

async function main() {
  // Read version from package.json
  const packageJsonPath = join(__dirname, '../package.json');
  const packageJson = JSON.parse(await readFile(packageJsonPath, 'utf8'));
  const version = packageJson.version;

  console.log(`[bake-version] Baking version ${version} into src/version.js`);

  // Write version to src/version.js
  const versionFilePath = join(__dirname, '../src/version.js');
  const versionFileContent = `// This file is auto-generated by bin/bake-version.js during npm pack
// DO NOT EDIT MANUALLY
export const SPARKLE_VERSION = '${version}';
`;

  await writeFile(versionFilePath, versionFileContent, 'utf8');
  console.log(`[bake-version] Version ${version} baked successfully`);
}

main().catch(error => {
  console.error('[bake-version] Error:', error.message);
  process.exit(1);
});
